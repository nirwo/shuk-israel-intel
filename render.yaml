services:
  - type: web
    name: shuk-israel-api
    runtime: node
    region: frankfurt
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "10000"
